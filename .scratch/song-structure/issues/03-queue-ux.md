# Analysis queue UX

Type: grilling
Status: open

## Question

Spec the on-demand + queue experience: context-menu entry naming, auto-queue rules when tracks join playlists (all playlists or opt-in per playlist?), queue visibility (progress in the existing analysis UI in `src/mainview/features/analysis`?), concurrency (one heavy job at a time?), cancellation, and failure surfacing.
