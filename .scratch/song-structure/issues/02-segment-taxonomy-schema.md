# Segment taxonomy, colors, and DB schema

Type: grilling
Status: open

Blocked by: 01

## Question

Given the label set all-in-one-fix actually emits (confirmed by the feasibility research), decide: which labels we keep vs merge, the color per segment type for the minimap lines, the local DB schema (segments table: track id, start, end, label, confidence?), versioning for re-analysis, and how segments coexist with drop-detector markers.
