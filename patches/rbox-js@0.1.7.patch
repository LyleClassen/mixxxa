diff --git a/index.js b/index.js
index 9b1139db6bce92c4b5e4924fd4fc2684c3a6fa67..396d6e5483bc7f77641da6da751bfbb38139abf6 100644
--- a/index.js
+++ b/index.js
@@ -3,8 +3,11 @@
 // @ts-nocheck
 /* auto-generated by NAPI-RS */
 
-const { createRequire } = require('node:module')
-require = createRequire(__filename)
+// Patched: removed `require = createRequire(__filename)` reassignment.
+// Bun's bundler hoists that to a global mutation of __require, which then
+// breaks the bundled native-binding require (it resolves relative to the
+// wrong base after this loader runs). Original line is unnecessary under
+// CJS — the module-local `require` parameter is already correct.
 
 const { readFileSync } = require('node:fs')
 let nativeBinding = null
