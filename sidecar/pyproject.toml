[project]
name = "orbit-sidecar"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "orbit-dsp==1.0.1",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = []
